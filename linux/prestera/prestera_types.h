#ifndef _PRESTERA_TYPES_H_
#define _PRESTERA_TYPES_H_

#include <stdbool.h>

#define u8 uint8_t
#define u16 uint16_t
#define u32 uint32_t
#define u64 uint64_t
#define __packed __attribute__ ((__packed__))
#define __aligned(a) __attribute__((aligned(a)))

#endif // _PRESTERA_TYPES_H_
